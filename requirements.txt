httpx>=0.27
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-dotenv>=1.0
python-multipart>=0.0.9
pandas>=2.0

# Keychain storage is macOS-only. On Linux hosts FFB_TOKEN_STORE=file is used
# and keyring is never imported, so it is not installed there.
keyring>=25.0; sys_platform == "darwin"
